import React from "react";

export default function UserComponent() {
  return (
    <div>
      <div>
        {/* {user.map(({ id, name, surname, email, ico, phone, dateOfBirth }) => (
          <p>{id}</p>
        ))} */}
        <h1> USERINFOLIST</h1>
        <h1> USERINFOLIST</h1>
        <h1> USERINFOLIST</h1>
        <h1> USERINFOLIST</h1>
      </div>
    </div>
  );
}
